{
  "name": "@agentgrit/core",
  "version": "0.1.4",
  "type": "module",
  "description": "Self-learning engine that makes AI agents smarter over time",
  "bin": {
    "agentgrit": "dist/agentgrit.js"
  },
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./capture": "./src/capture/index.ts",
    "./graph": "./src/graph/index.ts",
    "./adapters/*": "./src/adapters/*"
  },
  "files": [
    "dist/",
    "src/",
    "rubrics/",
    "agentgrit.config.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build bin/agentgrit.ts --outfile dist/agentgrit.js --target=node --minify",
    "prepublishOnly": "bun run build",
    "lint": "bunx biome check .",
    "typecheck": "bunx tsc --noEmit"
  },
  "peerDependencies": {
    "@huggingface/transformers": "latest"
  },
  "peerDependenciesMeta": {
    "@huggingface/transformers": {
      "optional": true
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@huggingface/transformers": "^3.5.1",
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hornjason/agentgrit.git"
  },
  "keywords": [
    "claude-code",
    "ai-agents",
    "self-learning",
    "feedback-loop"
  ]
}
