# capture/

Signal capture module. Hooks that fire during Claude Code sessions to capture user ratings, corrections, sentiment, skill invocations, and tool usage into JSONL signal files.

Registers as Claude Code hooks via `templates/hooks.json`.
