# adapters/

Data source adapters. Abstracts Langfuse, local JSONL storage, and Claude Code integration (hook registration, CLAUDE.md read/write, transcript parsing). Shared interfaces for Trace, Score, Signal, Rule, Pattern types.
