# detect/

Pattern detection module. Scans captured signals for recurring failure patterns, correlates low scores with specific behaviors, and stores high-rated session trajectories for contextual retrieval.
