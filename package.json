{
  "name": "@agentgrit/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Self-learning engine that makes AI coding agents improve over time",
  "bin": {
    "agentgrit": "./dist/agentgrit"
  },
  "files": [
    "dist/",
    "rubrics/",
    "agentgrit.config.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile bin/agentgrit.ts --outfile dist/agentgrit",
    "prepublishOnly": "bun run build",
    "lint": "bunx biome check .",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hornjason/agentgrit"
  },
  "keywords": [
    "claude-code",
    "ai-agents",
    "self-learning",
    "feedback-loop"
  ]
}
