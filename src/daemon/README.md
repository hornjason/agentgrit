# daemon/

Automation module. Background daemon loop (score, detect, promote, sync, optimize) running via LaunchAgent/systemd. Includes health check diagnostics, scheduler installer, and session lifecycle management.
